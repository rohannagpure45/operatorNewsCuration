# System packages required for PDF generation and browser automation
# These are installed via apt-get on Streamlit Cloud

# For playwright browser automation (headless Chrome)
chromium
chromium-driver

# For lxml parsing
libxml2-dev
libxslt1-dev
